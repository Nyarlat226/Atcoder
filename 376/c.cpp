#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main()
{
  int n;
  vector<int> a(n);
  vector<int> b(n - 1);
  int ans = -1;
  rep(i, n) cin >> a[i];
  rep(i, n - 1) cin >> b[i];
  rep(i, n)
  {
    rep9j,n-1)
    {
    }
  }
}
