#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main()
{
  int n;
  cin >> n;
  vector<int> k(n);
  rep(i, n) cin >> k[i];

  int mem = 0;
  rep(i, n) men += k[i];

  int a, b = 0;

  cout << max(a, b) << endl;
}
